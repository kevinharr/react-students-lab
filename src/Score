function Score(props) {
    return (
        <div>
            <h1>Scores: {props.studentList.scores} </h1>
            <ul>
                {props.studentList.scores.map(score => <li key={score}>{score}<li>)}}


            </ul>
        </div>
    );
}

export default Score;